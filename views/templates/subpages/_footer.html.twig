<footer id="footer" class="container"> 
    <h2>footer</h2>
    
    <div class="foot-box">
        <div class="timer" role="timer" aria-live="polite">
            <div class="catCSS">
                {% include "subpages/_catcss.html.twig" %}
            </div>
            <p>Temps écoulé: <span id="time">00:00:00</span></p>
        </div>
        
        <div class="retour-haut">
            <img id="backToTopButton" class="topButton" src="assets/img/Miaou/retourHautPage.png" alt="Retour en haut de page">
        </div>
    </div>
    
    <div class="foot-content">
        <div class="footer-flex-container">
            <div class="footer-top">
                <a href="index.php?route=accueil"><img src="assets/img/Miaou/JouerAvecMiaouLogo.png" alt="Logo chat"></a>
                <div class="footer-address">
                    <p></p>
                    <p>9 La Guermonnais</p>
                    <p>35720 Mesnil-Roc'h</p>
                    <div class="footer-tel-container">
                        <p><i class="fa-sharp fa-solid fa-phone"></i>06.21.03.27.13</p>
                    </div>
                    <div class="footer-mail-container">
                        <p><i class="fa-solid fa-envelope"><a href="{{ 'mailto:apprendreavecmiaou@gmail.com' | escape('html') }}"></a></i><a href="?route=contact">Contact</a></p>
                    </div>
                </div>
            </div>
            <div class="footer-separation">
            </div>
            <div class="footer-bottom">
                <div class="footer-nav">

                    {% set canAccess = isUser or (user.role == 1 and user.statut == 1) %}

                    <p>Vous êtes sur la page <strong>{{ titre }}</strong></p>
                    <ul>
                        {% if canAccess %}
                            {# Liens pour les utilisateurs connectés #}
                            {% if titre != 'Accueil' %}
                                <li><a href="?route=homepageUser">Accueil</a></li>
                            {% endif %}

                            {% if titre != 'Coloriages' %}
                                <li><a href="?route=drawing">Coloriages</a></li>
                            {% endif %}

                            {% if titre != 'Jeux' %}
                                <li><a href="?route=games">Jeux</a></li>
                            {% endif %}

                            {% if titre != 'Histoires' %}
                                <li><a href="?route=stories">Histoires</a></li>
                            {% endif %}

                            {% if titre != 'Profil' %}
                                <li><a href="?route=profile">Profil</a></li>
                            {% endif %}
                            {% if titre != 'Notre Philosophie' %}
                                <li><a href="?route=pedagogie">Notre philosophie</a></li>
                            {% endif %}

                        {% else %}
                            {# Liens pour les visiteurs non connectés #}
                            {% if titre != 'Accueil' %}
                                <li><a href="?route=homepage">Accueil</a></li>
                            {% endif %}

                            {% if titre != 'Contact' %}
                                <li><a href="?route=contact">Contact</a></li>
                            {% endif %}

                            {% if titre != 'Notre Philosophie' %}
                                <li><a href="?route=pedagogie">Notre philosophie</a></li>
                            {% endif %}
                        {% endif %}
                    </ul>

                </div>
                <div class="footer-rs-legal">
                    <ul>
                        <li>
                            <a href="">Politique de confidentialité</a>
                        </li>
                        <li>
                            <a href="">Conditions générales de vente</a>
                        </li>
                        <li>
                            <a href="">Mentions légales</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <div class="copyright" id="copyright">
        <div>
            <p>&copy;Apprendre avec Miaou - Laure Bonnevalle, 2025. Tous droits réservés.</p>
        </div>
    </div>
</footer>
